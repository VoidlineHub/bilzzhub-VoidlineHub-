-- [[ 🛡️ BYPASS ADONIS & NETWORK ]] --
local genv = getgenv()
pcall(function()
    loadstring(game:HttpGet('https://raw.githubusercontent.com/LynX99-9/komtolmmek2/refs/heads/main/Adonis'))()
    if game:GetService('CoreGui').RobloxGui:FindFirstChild('CoreScripts/NetworkPause') then
        game:GetService('CoreGui').RobloxGui['CoreScripts/NetworkPause']:Destroy()
    end
end)

-- [[ ⚙️ SETTINGS ]] --
local LP = game:GetService("Players").LocalPlayer
local RS = game:GetService("ReplicatedStorage")
local TS = game:GetService("TweenService")

local POS_A = CFrame.new(80.019, -4.013, -872.866)
local POS_B = CFrame.new(1300.53, -4.013, -867.006)
local MOTOR_NAME = "Yamahax-MioSporty"

genv.VoidlineAutoFarm = false
genv.VoidlineSpeed = 250 -- Default Speed (KM/H)
local RESET_INTERVAL = 7 * 60 -- 7 Menit dalam detik
local StartMoney = 0
local StartTime = 0
local CurrentEarning = 0

-- [[ 💰 FUNGSI FORMAT RP ]] --
-- Fungsi ini buat bikin angka jadi "Rp 100,000,000"
local function formatRP(p2)
    local v3, v4, v5 = string.match(tostring(p2), "^([^%d]*%d)(%d*)(.-)$")
    if v3 and (v4 and v5) then
        return "Rp " .. v3 .. v4:reverse():gsub("(%d%d%d)", "%1,"):reverse() .. v5
    else
        return "Rp " .. tostring(p2)
    end
end

-- [[ 🚀 QUADRA TWEEN SYSTEM (KM/H LOGIC) ]] --
local function VoidlineVehicleTween(targetCFrame)
    local char = LP.Character
    local seat = char and char.Humanoid.SeatPart
    if not seat then return end
    
    local car = seat.Parent
    local primary = car.PrimaryPart or seat
    
    -- Konversi KM/H ke Studs per Second (Roblox Scale)
    local studsPerSecond = genv.VoidlineSpeed / 1.5 -- Rasio biar berasa kencengnya pas
    local distance = (primary.Position - targetCFrame.p).Magnitude
    local duration = distance / studsPerSecond 
    
    local oldGravity = workspace.Gravity
    workspace.Gravity = 0
    
    local tweenValue = Instance.new("CFrameValue")
    tweenValue.Value = primary.CFrame
    
    local connection = tweenValue.Changed:Connect(function()
        car:PivotTo(tweenValue.Value)
    end)
    
    local tween = TS:Create(tweenValue, TweenInfo.new(duration, Enum.EasingStyle.Linear), {Value = targetCFrame})
    tween:Play()
    tween.Completed:Wait()
    
    connection:Disconnect()
    tweenValue:Destroy()
    workspace.Gravity = oldGravity
end

-- [[ 🔄 MAIN EXECUTION ]] --
local function StartVoidlineFarm()
    local lastReset = tick()

    while genv.VoidlineAutoFarm do
        local char = LP.Character
        local hum = char:WaitForChild("Humanoid")
        local hrp = char:WaitForChild("HumanoidRootPart")

        -- 1. CEK TIMER 7 MENIT ATAU LAGI TURUN MOTOR
        if tick() - lastReset >= RESET_INTERVAL or not hum.SeatPart then
            print("🌑 Voidline: Time to Reset / Boarding...")
            if hum.SeatPart then hum.Jump = true task.wait(0.5) end
            
            RS.SpawnCarEvents.SpawnCar:FireServer(MOTOR_NAME)
            task.wait(1.5)
            
            for _, v in pairs(workspace:GetChildren()) do
                if (v.Name:find("blog827383") or v.Name:find("Montors")) and v:FindFirstChild("DriveSeat") then
                    hrp.CFrame = v.DriveSeat.CFrame * CFrame.new(0, 1.5, 0)
                    task.wait(0.3)
                    fireproximityprompt(v.DriveSeat:FindFirstChildOfClass("ProximityPrompt"))
                    break
                end
            end
            lastReset = tick() -- Reset timer
            task.wait(1)
        end

        -- 2. TWEEN BOLAK-BALIK
        if hum.SeatPart then
            pcall(function()
                VoidlineVehicleTween(POS_B)
                task.wait(0.5)
                VoidlineVehicleTween(POS_A)
                task.wait(0.5)
            end)
        end
        task.wait(0.1)
    end
end

-- [[ 🎨 UI WINDUI ]] --
local WindUI = loadstring(game:HttpGet('https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua'))()

local Window = WindUI:CreateWindow({
    Title = "VoidlineHub",
    Icon = "house", 
    Author = "Bilzz",
    Folder = "Voidline_Configs", -- Folder buat simpan settingan slider/toggle
    Transparent = true,
    Size = UDim2.fromOffset(300, 350),
    Topbar = {
        Height = 45,
        ButtonsType = "Mac",
    },
    OpenButton = {
        Enabled = true,
        Title = "Open Menu",
        Draggable = true,
        Color = ColorSequence.new(Color3.fromRGB(45, 90, 45), Color3.fromRGB(20, 50, 20))
    }
})

-- [[ 🏎️ TAB AUTO FARM ]] --
local Tab = Window:Tab({ 
    Title = "Auto Farm", 
    Icon = "zap",
    Selected = true -- WAJIB: Biar isinya langsung nampil
})

local EconSection = Tab:Section({ Title = "auto drag race" })
-- [[ LANJUTIN TOGGLE & SLIDER DI BAWAH SINI ]] --
Tab:Toggle({
    Title = "Start Smart Farm",
Desc = "ini paling gacor si",
    Default = false,
    Callback = function(v)
        genv.VoidlineAutoFarm = v
        if v then task.spawn(StartVoidlineFarm) end
    end
})

Tab:Slider({
    Title = "Speed (KM/H)",
    Desc = "Pakai senormalnya aja",
    IsTooltip = true,
    IsTextbox = false,
    Step = 1,
    Value = {
        Min = 0,
        Max = 500,
        Default = 250,
    },
    Callback = function(value)
        genv.VoidlineSpeed = value
    end
})

-- [[ 📊 TAB STATS ]] --
local StatsTab = Window:Tab({ Title = "Dashboard", Icon = "line-chart", Selected = false })

-- Section 1: Economy & Time
local EconSection = StatsTab:Section({ Title = "Economy Monitor" })

local RunTimeLabel = EconSection:Label({ Title = "Running Time: 00:00:00" })
local EarningLabel = EconSection:Label({ Title = "Total Earning: $0" })
local CashLabel = EconSection:Label({ Title = "Current Money: $0" })
local RPLabel = EconSection:Label({ Title = "Current RP: 0" })

-- Section 2: System Info
local SystemSection = StatsTab:Section({ Title = "System Info" })

local FpsLabel = SystemSection:Label({ Title = "FPS: 0" })
local PingLabel = SystemSection:Label({ Title = "Ping: 0ms" })
local ExecLabel = SystemSection:Label({ Title = "Executor: " .. (identifyexecutor() or "Unknown") })

-- [[ 🔄 STATS REFRESHER LOGIC ]] --
task.spawn(function()
    while true do
        task.wait(1) 
        
        -- Ambil data dari PlayerData sesuai yang lu kasih tadi
        local PlayerData = LP:WaitForChild("PlayerData")
        local CurrentRP = PlayerData.RPValue.Value 
        
        -- 1. LOGIC EARNING (Hanya kalau AutoFarm ON)
        if genv.VoidlineAutoFarm then
            if StartRP == 0 then 
                StartRP = CurrentRP -- Catat RP awal pas klik ON
                StartTime = tick()
            end
            CurrentEarning = CurrentRP - StartRP
            
            -- Timer Formatter (Jam:Menit:Detik)
            local diff = tick() - StartTime
            local h = math.floor(diff / 3600)
            local m = math.floor((diff % 3600) / 60)
            local s = math.floor(diff % 60)
            
            RunTimeLabel:SetTitle(string.format("Running Time: %02d:%02d:%02d", h, m, s))
            EarningLabel:SetTitle("Total Earning: " .. formatRP(CurrentEarning))
        else
            -- Reset biar kalau dimatiin terus dinyalain lagi, itungannya mulai dari 0
            StartRP = 0
            StartTime = 0
        end
        
        -- 2. UPDATE DISPLAY LABELS
        CashLabel:SetTitle("Current Money: " .. formatRP(CurrentRP))
        
        -- 3. UPDATE SYSTEM INFO (FPS & PING)
        local fps = math.floor(1 / game:GetService("RunService").RenderStepped:Wait())
        local ping = math.floor(game:GetService("Stats").Network.ServerStatsItem["Data Ping"]:GetValue())
        
        FpsLabel:SetTitle("FPS: " .. fps)
        PingLabel:SetTitle("Ping: " .. ping .. "ms")
    end
end)
